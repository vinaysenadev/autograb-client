// import styles from "./layout.module.css";
import logo from "../assets/logo.png";

const Header = () => {
  return (
    <nav>
      <img src={logo} alt="auto grab logo" height={30} />
    </nav>
  );
};

export default Header;
